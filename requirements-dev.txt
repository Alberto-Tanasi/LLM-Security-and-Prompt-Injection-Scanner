# Additional dependencies needed only to run the test suite (tests/).
# Not required to actually use the scanner -- see requirements.txt for that.
#
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# Run tests with: pytest -v

pytest>=7.4.0
